create kerasFeatures csv files with pooling layers to reduce Featurevector length


reduce length of keras featurevectors (pooling layers)


add timing to Bitsampling stuff to find out what of it is so slow