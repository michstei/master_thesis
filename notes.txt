do runs with 512Avg and 512Max Features 


reduce length of keras featurevectors (pooling layers) --> done with dense layer
    create kerasFeatures csv files with pooling layers to reduce Featurevector length --> done with dense layer

